@model IEnumerable<BookDetails>
@*<html>
    <head>
        <title> Get All Bokks</title>
    </head>
    <body>*@

@*@{
        Layout ="~/Views/Shared/_Layout.cshtml";
    }*@

@{
    ViewBag.Title = "Index";
}
@if (Model.Count() == 0)
{
    <h3>
        No Books
    </h3>
}
else
{
    <br />
    <button class="btn btn-outline-info float-left"> @Html.ActionLink("Add New", "CreateBook", "book", null, new { @class = "nav-link" }) </button>
   
    <br />

    <h2 style="text-align:center;color:brown;">Available Books</h2>
    <br />
    @*<button class="btn btn-outline-primary float-right">@Html.ActionLink("Update", "Update", new { id = Model.Id }, null)</button>
        @using (Html.BeginForm("Delete", "Book", new { id = Model.id }, FormMethod.Post))
        {
            <button id="CmdDelete" type="submit" class="btn btn-danger  float-right ">Delete</button>
        }*@



    <ul class="list-group">
        @foreach (var BookDetails in Model)
        {
            <li class="list-group-item">
                <a asp-controller="Book" asp-action="Details" asp-route-id="@BookDetails.Id">@BookDetails.BookName</a>

                <button class="btn btn-outline-primary float-right"> @Html.ActionLink("Update", "UpdateBook", "book", new { id = BookDetails.Id }, new { @class = "nav-link" }) </button>

                @*@book.BookName*@
            </li>
        }
    </ul>
}
@*</body>
    </html>*@
